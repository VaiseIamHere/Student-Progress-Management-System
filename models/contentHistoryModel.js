import mongoose from "mongoose"

const ContestHistorySchema = mongoose.Schema(
    {
        
    }
)

const ContestHistoryModel = mongoose.model("ContestHistory", ContestHistorySchema)

export default ContestHistoryModel
